/** CV in `public/dipendra l cv.pdf` — opens in a new browser tab from the hero. */
export const CV_PDF_FILENAME = "dipendra l cv.pdf";

export const CV_PDF_PATH =
  "/" + encodeURIComponent(CV_PDF_FILENAME).replace(/%2F/g, "/");
